package gg.circular;

import gg.binaryTree.TreeNode;
import gg.binaryTree.TreeNodeInsertion;
import gg.single.Node;

public class circulationLinkedListConvertion {

	public static void main(String args[]){
		TreeNode n1=new TreeNode(10);
		TreeNodeInsertion.insertNode(n1, data);
		
		Node head=null;
		circularLinkedListInsertion.printData(head);

	}	
}
