package gg.binaryTree;

public class TreeNodeDiameter {

		public static void main(String args[]){
			TreeNode head=TreeNodeInsertion.insertNode(null,1);
			head.left=new TreeNode(2);
			head.right=new TreeNode(3);
			head.left.left=new TreeNode(4);
			head.left.right=new TreeNode(5);
			System.out.print("Iterative: ");
			maxDiameter(head);
		}

		public static int maxDiameter(TreeNode head, int maxD){
			
			if(head==null)
				return 0;
			
			int left=maxDiameter(head.left,maxD);
			int right=maxDiameter(head.right,maxD);
			int maxHeight;
			if(left>right){
				
			}
			maxD=left+right+1
			return maxD;
		}
	}
