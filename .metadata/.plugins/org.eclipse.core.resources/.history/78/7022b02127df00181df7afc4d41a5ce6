package gg.binaryTree;

public class TreeNodeInsertion {
	
	public Node insertNode(TreeNode head,int data){
		if(head==null){
			return new TreeNode(data);
		}
		if(head.data<data){
			head.right=insertNode(head.right, data);
		}
		
		
		return null;
	}
}
